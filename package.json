{
  "name": "forge-server",
  "private": true,
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^20.12.12"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only index.ts || node --watch index.ts"
  }
}